<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

    
<mapper namespace="com.checkin.web.dao.MemberDao">
	
	<select id="getByEmail" resultType="com.checkin.web.entity.Member">
        select * from member where email = #{email}
     </select>
	
	<select id="get" resultType="com.checkin.web.entity.Member">
        select * from member where id = #{id}
     </select>
      
     <select id="getCount" resultType="int">
        select count(id) from member
      	   <where>
	          <if test="query != null">nickname like '%${query}%'</if>
	          <if test="position != null">and  position = '#{position}</if>
    		</where>
     </select>
     
     <select id="getList" resultType="com.checkin.web.entity.Member">
        select * from member
		   <where>  
	          <if test="query != null">nickname like '%${query}%'</if>
	          <if test="position != null">and  position = '#{position}</if>
		   </where>
     </select> 
        
     <insert id="insert" parameterType="com.checkin.web.entity.Member">
     	insert into member(nickname, password, email, position_id)
     	values(#{nickname}, #{password}, #{email}, '3')
     </insert>
     
     <update id="update" parameterType="com.checkin.web.entity.Member">
     	update
     	<set>
			<if test="nickname != null"> nickname = #{nickname},</if>
			<if test="password != null"> password = #{password},</if>
			<if test="email != null"> email = #{email},</if>
			<if test="profileImg != null"> profile_img = #{profileImg},</if>
			<if test="positionId != null"> position_id = #{positionId}</if>
     	</set>
     	where id=#{id}
     </update>
     
     <delete id="delete" parameterType="int">
     	delete from member where id = #{id} 
     </delete>
</mapper>